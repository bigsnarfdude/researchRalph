# Worktrees (created at runtime)
worktrees/

# Results (collected separately)
results/

# Runtime artifacts
*.log
run.log
agent.log
result.json
*.pyc
__pycache__/

# Editor
.DS_Store
.vscode/
.idea/

# Conductor state
.conductor/

# v5 guard / loop transient state (never commit run scratch)
**/GUARD_HALT
**/.oracle_hash
**/.mock_calls
**/.distill_at_rows
**/runs/
v5/cost_ledger.tsv
v5/.test_ledger.tsv
v5/.live_ledger.tsv

# Agent scratch (created at runtime in worktrees)
memory/
scratch/

# Python
.venv/
*.egg-info/
dist/
build/
sft_data/attempts_*
sft_data/*.tar.gz
sft_data/*.jsonl
sft_data/*.tsv
sft_data/*.md
# model artifacts — never in git (62GB incident, June 2026)
domains/rrma-r1/checkpoints/
domains/rrma-r1/best/
*.npz
*.pt
*.safetensors
*.ckpt
.kviol_private/
